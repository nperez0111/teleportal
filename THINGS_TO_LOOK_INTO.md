- Right now, transforms are defined as `AsyncIterable<T[]>`, but is this really what we want? I think a more powerful abstraction is actually to have a sort of QoS system, so that things that are known to not be durable, e.g. an awareness message can be considered LWW and if you can save transmitting a batch (if it has not yet been consumed), then you would save overall time in the system. But, maybe treating certain messages like this, in a different way, might add complexity to the system, for minimal cost of saving a message transmission for an event that is likely of little cost or consquence.
  - Written up in `docs/decisions/001-message-coalescing.md`: decided against for now. The `compact` hook already exists and is unused; the expensive part is the per-client outbound queues it would need, and awareness is already ack-free, ephemeral, and separately rate-limited. Doc names the measurement that would flip it.
-
