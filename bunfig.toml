[serve.static]
plugins = ["bun-plugin-tailwind"]

[install]
# Use isolated installs to prevent dependency hoisting in workspaces
# This ensures each workspace (examples/*, showcase, docs) has its own node_modules
linker = "isolated"
