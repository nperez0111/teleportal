# Observability

This guide demonstrates the built-in observability endpoints provided by Teleportal's HTTP handlers for monitoring and health checks.

## What it demonstrates

- Accessing the `/health` endpoint for server health checks
- Accessing the `/metrics` endpoint for Prometheus-compatible metrics
- Accessing the `/status` endpoint for server status information
- Using observability endpoints in production deployments for monitoring
